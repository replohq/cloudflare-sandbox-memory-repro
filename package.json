{
  "name": "sandbox-repro",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bash scripts/start-minio.sh && wrangler dev",
    "deploy": "wrangler deploy",
    "build:container": "docker build -t sandbox-repro -f Dockerfile .",
    "repro": "npx tsx repro.mts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "@types/node": "^22.13.4",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "wrangler": "^4.6.0"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.7.6"
  }
}
